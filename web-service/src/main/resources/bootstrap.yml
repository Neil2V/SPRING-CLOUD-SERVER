spring:
  cloud:
    config:
      uri: http://localhost:8081
      enabled: true
      fail-fast: true
  application:
    name: web-service
  config:
    import: optional:configserver:http://localhost:8081
