server:
  port: 8081
spring:
  cloud:
    config:
      enabled: true
      server:
        git:
          uri: E:\workspace\SPRING-CLOUD-SERVER
          search-paths: config-data
  application:
    name: cloud-server

